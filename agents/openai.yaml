interface:
  display_name: "UI Compose"
  short_description: "Compose host-native product UI from proven patterns"
  default_prompt: "Use $ui-compose to read the host frontend stack and design system, choose an appropriate page skeleton and a small coherent pattern set, adapt them to the host primitives and tokens without unnecessary UI dependencies, then verify responsive, accessibility, state, motion, and runtime behavior."
